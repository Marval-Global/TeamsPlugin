on:
  push:
    branches:
    - main

jobs:
  my_job:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Zip files
        run: zip -r Teams.zip ${{ github.workspace }}/ -i .
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Release
          path: Teams.zip
